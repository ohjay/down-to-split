<%= simple_form_for(@purchase) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">

    <%= f.input :cost %>

    <%= f.association :product %>

    <%= f.association :shopping_trip %>

  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
